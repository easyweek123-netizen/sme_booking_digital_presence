import { Box } from '@chakra-ui/react';
import { motion } from 'framer-motion';

/**
 * Chakra UI Box with Framer Motion capabilities
 * Use this for animated containers throughout the app
 */
export const MotionBox = motion.create(Box);

